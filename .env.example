# Copy to .env (chmod 600). Loaded via python-dotenv.
OPENROUTER_API_KEY=      # story generation, spiral transcripts, LLM judge
HF_TOKEN=                # gated Gemma checkpoints + uploading results
HF_USER=timf34
RUNPOD_API_KEY=          # only needed on the laptop for `rp up`
